# AutoSense AI

**[English]**  
AutoSense AI is a local, privacy-focused car benchmarking application powered by Retrieval-Augmented Generation (RAG). It allows users to compare vehicle specifications, pricing, and performance metrics using natural language queries, without relying on external APIs.

**[Türkçe]**  
AutoSense AI, RAG (Retrieval-Augmented Generation) teknolojisi ile güçlendirilmiş, yerel ve gizlilik odaklı bir araç kıyaslama uygulamasıdır. Kullanıcıların doğal dil sorguları kullanarak araç özelliklerini, fiyatlarını ve performans verilerini, harici API'lara bağımlı olmadan karşılaştırmasını sağlar.

---

## Features / Özellikler

### English
- **Offline RAG Architecture:** Runs entirely on a local LLM (TinyLlama-1.1B), ensuring full privacy and zero API costs.
- **Spec-Aware Comparisons:** Understands technical details like Horsepower, MSRP, MPG, Transmission, and Dimensions.
- **Premium UI:** Modern, dark-themed interface built with Next.js and Glassmorphism design principles.
- **Vector Search:** Uses ChromaDB for semantic retrieval of over 11,000 car records.

### Türkçe
- **Çevrimdışı RAG Mimarisi:** Tamamen yerel bir LLM (TinyLlama-1.1B) üzerinde çalışır, tam gizlilik ve sıfır API maliyeti sağlar.
- **Teknik Karşılaştırma:** Beygir gücü, fiyat (MSRP), yakıt tüketimi, şanzıman ve boyutlar gibi teknik detayları anlar.
- **Premium Arayüz:** Next.js ve Glassmorphism prensipleriyle tasarlanmış modern, karanlık temalı arayüz.
- **Vektör Arama:** 11.000'den fazla araç kaydının anlamsal araması için ChromaDB kullanır.

---

## Technology Stack / Teknoloji Yığını

- **Frontend:**
  - Next.js 14 (App Router)
  - TypeScript
  - Tailwind CSS
  - Lucide React (Icons)
  
- **Backend:**
  - FastAPI (Python Framework)
  - LangChain (Orchestration)
  - ChromaDB (Vector Database)
  - Sentence-Transformers (`all-MiniLM-L6-v2`)
  - CTransformers (Local LLM Runtime)
  - Pandas (Data Processing)

---

## 📂 Project Structure / Proje Yapısı

```
AutoSense/
├── backend/
│   ├── chroma_db/       # Vector database storage (Vektör veritabanı dosyaları)
│   ├── env/             # Virtual environment (Sanal ortam)
│   ├── data.csv         # Raw vehicle dataset (Ham veri seti)
│   ├── ingest.py        # Data processing script (Veri işleme ve indeksleme)
│   ├── main.py          # FastAPI server entry point (API sunucusu)
│   ├── rag.py           # RAG pipeline & LLM logic (RAG ve LLM mantığı)
│   └── requirements.txt # Python dependencies (Bağımlılıklar)
│
├── frontend/
│   ├── app/             # Next.js pages & layout (Sayfalar ve düzen)
│   ├── components/      # React components (ChatInterface) (Bileşenler)
│   └── public/          # Static assets (Statik dosyalar)
```

---

## 🧠 System Logic / Sistem Mantığı

### 1. Data Ingestion (Veri İşleme)
**[EN]**  
The `ingest.py` script reads the raw `data.csv`. It cleanses the data and converts each row into a natural language description (e.g., *"The 2017 Toyota 86 has 205 HP..."*). These descriptions are converted into vector embeddings and stored in `ChromaDB`.

**[TR]**  
`ingest.py` betiği ham `data.csv` dosyasını okur. Veriyi temizler ve her satırı doğal bir dil açıklamasına dönüştürür (örn: *"2017 Toyota 86, 205 beygir gücüne sahiptir..."*). Bu açıklamalar vektör gömülmelerine (embeddings) dönüştürülür ve `ChromaDB` içinde saklanır.

### 2. RAG Pipeline (RAG Hattı)
**[EN]**  
When a user asks a question:
1.  **Retrieval:** The query is embedded, and the most relevant 5 cars are fetched from ChromaDB.
2.  **Context Construction:** These car details are formatted into a context block.
3.  **Generation:** The Local LLM (TinyLlama) receives the user question + the retrieved car data and generates a factual answer.

**[TR]**  
Kullanıcı bir soru sorduğunda:
1.  **Erişim (Retrieval):** Sorgu vektöre dönüştürülür ve ChromaDB'den en alakalı 5 araç getirilir.
2.  **Bağlam Oluşturma:** Bu araç detayları bir bağlam bloğu olarak düzenlenir.
3.  **Üretim (Generation):** Yerel LLM (TinyLlama), kullanıcı sorusunu ve getirilen araç verilerini alarak, doğruluğu yüksek bir cevap üretir.

---

## Setup / Kurulum

### Backend
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python ingest.py  # Create database / Veritabanını oluştur
python main.py    # Start server / Sunucuyu başlat
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

---

*Generated by AutoSense AI Agent.*
